-- generated by  tools/unquote.py qft.42 aqft 2
-- do not edit; edit qft.42 and regenerate

def x      = swapsum
def mat    = dist ; (unitprod + unitprod)
def ctrl m = mat ; (id + m) ; mat!
def s      = id + (omega ; omega)
def t      = id + omega
def z      = id + (omega ; omega ; omega ; omega)
def hcore  = x ; s ; v ; s ; x
def h      = unitprod! ; (omega * hcore) ; unitprod

def qft3 = ((h * id) ; (ctrl ((s * t)) ; (id * ((h * id) ; (ctrl (s) ; (id * h))))))
